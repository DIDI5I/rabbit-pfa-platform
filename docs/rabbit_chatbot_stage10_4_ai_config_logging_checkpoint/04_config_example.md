# config.example.php

A safe template config should exist in the repo:

```text
config.example.php
```

It should include no real secrets.

Use blank values for:

```text
database password
AI API key
```

Purpose:

```text
developers can copy config.example.php to config.php locally
real config.php remains ignored
```
